# DrissionPage聊天历史爬取工具使用说明

## 概述

基于DrissionPage的聊天历史爬取工具是一个使用浏览器自动化技术来获取闲鱼聊天记录的工具。相比API方式，这种方法可以绕过API限制，获取真实的聊天历史记录。

## 功能特性

- ✅ 浏览器自动化访问闲鱼聊天页面
- ✅ 手动登录支持
- ✅ 聊天列表获取
- ✅ 特定用户聊天历史爬取
- ✅ 多种导出格式支持（JSON、Markdown）
- ✅ 无头模式支持
- ✅ 集成到主菜单

## 使用方法

### 1. 通过主菜单使用

最简单的方式是通过主菜单使用：

```bash
python tools/main_menu.py
```

然后选择选项 `4. 用户对话历史爬取(浏览器方式)`

### 2. 命令行直接使用

```bash
# 基本用法
python tools/drissionpage_chat_crawler.py <用户ID>

# 完整参数
python tools/drissionpage_chat_crawler.py <用户ID> \
    --output-dir <输出目录> \
    --max-messages <最大消息数> \
    --format <输出格式> \
    --headless

# 示例
python tools/drissionpage_chat_crawler.py 123456789 \
    --output-dir my_chats \
    --max-messages 50 \
    --format all \
    --headless
```

### 3. 参数说明

- `用户ID` (必需): 要爬取聊天记录的用户ID
- `--output-dir`: 输出目录，默认 `drissionpage_chats`
- `--max-messages`: 最大消息数量，默认 `50`
- `--format`: 输出格式，可选 `json`、`markdown`、`all`，默认 `all`
- `--headless`: 使用无头模式（不显示浏览器界面）

## 使用步骤

1. **启动工具**: 通过主菜单或命令行启动工具
2. **浏览器初始化**: 工具会自动初始化浏览器
3. **手动登录**:
   - 浏览器会打开闲鱼聊天页面
   - 请手动完成登录操作
   - 登录完成后按回车键继续
4. **爬取聊天记录**: 工具会自动获取指定用户的聊天历史
5. **导出文件**: 聊天记录会自动保存到指定目录

## 输出文件

工具会生成以下格式的文件：

### JSON格式
```json
{
  "user_id": "123456789",
  "user_name": "用户名",
  "chat_id": "chat_123456789",
  "item_id": "",
  "item_title": "",
  "messages": [
    {
      "role": "user",
      "content": "消息内容",
      "timestamp": 1731326400,
      "message_id": "msg_001"
    }
  ],
  "created_at": 1731326400,
  "last_updated": 1731326400
}
```

### Markdown格式
```markdown
# 闲鱼聊天记录 - 用户 用户名

**用户ID**: `123456789`
**聊天ID**: `chat_123456789`
**商品ID**: ``
**商品标题**:
**消息总数**: 5
**导出时间**: 2025-11-11 15:30:00

---

## 聊天记录

### 消息 1 - 2025-11-11 15:25:00
**👤 用户**:

> 你好，这个商品还在吗？

---
```

## 注意事项

1. **登录要求**: 需要手动登录闲鱼账号
2. **用户ID获取**: 需要提前知道要爬取的用户ID
3. **网络稳定性**: 需要稳定的网络连接
4. **浏览器兼容性**: 需要安装Chrome浏览器
5. **反爬虫机制**: 请合理使用，避免触发闲鱼的反爬虫机制

## 故障排除

### 浏览器初始化失败
- 检查是否安装了Chrome浏览器
- 检查网络连接
- 尝试重启工具

### 登录失败
- 确保手动完成登录操作
- 检查登录页面是否正常加载
- 确保登录后页面跳转到聊天页面

### 无法获取聊天记录
- 确认用户ID是否正确
- 检查是否与目标用户有聊天记录
- 尝试使用非无头模式查看浏览器状态

## 技术说明

### 工作原理
1. 使用DrissionPage库控制Chrome浏览器
2. 访问闲鱼聊天页面 `https://www.goofish.com/im`
3. 等待用户手动登录
4. 解析HTML页面结构提取聊天信息
5. 将数据导出为指定格式

### 依赖库
- `DrissionPage`: 浏览器自动化库
- `loguru`: 日志记录
- 标准库: `os`, `json`, `time`, `datetime`

## 与API方式的对比

| 特性 | API方式 | 浏览器方式 |
|------|---------|------------|
| 获取方式 | 通过闲鱼API | 浏览器自动化 |
| 成功率 | 较低（API端点不存在） | 较高 |
| 实时性 | 实时 | 实时 |
| 复杂度 | 简单 | 中等 |
| 依赖 | 网络请求 | 浏览器 |
| 反爬虫 | 容易触发 | 相对稳定 |

## 更新日志

- **v1.0.0** (2025-11-11): 初始版本发布
  - 基础浏览器自动化功能
  - 聊天记录爬取和导出
  - 主菜单集成